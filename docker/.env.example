# AI Docker CLI Setup - Environment Configuration
# Copy this file to .env and modify as needed
# The setup wizard will create .env automatically with your settings

# ============================================================
# REQUIRED: User Configuration (set by setup wizard)
# ============================================================

# Username for the container (Linux user that will be created)
USER_NAME=your_username

# Password - use ONE of these options:
# Option 1: Pre-hashed password (SHA-512 format: $6$salt$hash)
USER_PASSWORD_HASH=

# Option 2: Plain text password (will be hashed on first run)
USER_PASSWORD_PLAIN=your_password

# Path to your workspace directory on Windows (will be mounted to /workspace)
WORKSPACE_PATH=C:\Users\YourName\Documents\AI_Work

# ============================================================
# OPTIONAL: Service Configuration
# ============================================================

# Vibe Kanban web UI port (default: 3000)
VIBE_KANBAN_PORT=3000

# Force reinstall of CLI tools on next container start
# Set to 1 to reinstall all tools, then remove after restart
FORCE_CLI_REINSTALL=

# ============================================================
# DEVELOPER OPTIONS
# ============================================================

# Developer Mode - mounts scripts for live editing (default: 0)
# Set to 1 if you are developing this tool and want to edit scripts
# without rebuilding the Docker image
# When enabled, uses docker-compose.dev.yml overlay
DEV_MODE=0
